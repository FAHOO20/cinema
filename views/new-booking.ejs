<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("partials/head", { pageTitle: "Book a Ticket" }) %>
</head>
<body>
  <%- include("partials/header") %>
  <main>
    <h1>New Booking</h1>
    
    <form action="/web/booking" method="POST">
      <!-- Hidden fields for "movie" and "user" so user doesn't type them manually -->
      <input type="hidden" name="movie" value="<%= selectedMovieId %>" />
      <input type="hidden" name="user" value="<%= loggedInUserId %>" />

      <label>Date (YYYY-MM-DD):</label><br/>
      <input type="date" name="date" required /><br/><br/>

      <label>Seat Number:</label><br/>
      <input type="number" name="seatNumber" required min="1" /><br/><br/>

      <button type="submit">Create Booking</button>
    </form>
  </main>
  <%- include("partials/footer") %>
</body>
</html>
